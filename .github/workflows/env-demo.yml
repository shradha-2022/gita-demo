name: Env Demo

on:
  push

env:
  APP_ENV: "dev"

jobs:
  env_demo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: connecting to Env
        run: echo "App is connecting to $APP_ENV"
